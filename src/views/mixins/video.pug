mixin video(test)
    div
        h4
            a(href=`/videos/${test.id}`)=test.title
        ul
            li #{test.rating} / 5
            li #{test.comments} comments
            li Posted #{test.createdAt}
            li #{test.views} views